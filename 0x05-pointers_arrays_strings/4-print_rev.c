#include "main.h"

/**
 * print_rev - ptints str in rev
 * @s: char to be revesed
 * Return: returns
 */

void print_rev(char *s)
{
	int longi = 0;
	int x;

	while (*s != '\0')
	{
		longi++;
		s++
	}
	s--;
	for (o = longi; O > 0 O--)
	{
		_putchar(*s);
		s--;
	}
	_putchar('\n');
}
