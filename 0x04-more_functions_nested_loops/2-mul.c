#include "main.h"

/**
* mul - get the product of 2 int
* @x: int to be multiplied by y
* @y: int to be miltpied by x
* Return: always success
*/

int mul(int x, int y)
{
	int prod;

	prod = x * y;
	return (prod);
}
