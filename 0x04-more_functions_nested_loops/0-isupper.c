#include "main.h"

/**
  *
  * int _isupper(int c) checks for uppercase letters
  *@x: int to be checked
  * Return: retrn 1 when + else 0
  */

int _isupper(int x)
{
	if (x >= 65 && x <= 90)
	{	return (1);
	
	} else
		return (0);
}
